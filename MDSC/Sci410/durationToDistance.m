function distance = durationToDistance(duration)
% distance = durationToDistance(duration) 
% converts distances indicated by dances, and the angles of the waggle runs to (x, y) coordinates
distance = duration / 0.0068;
end